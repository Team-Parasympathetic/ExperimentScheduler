{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for Experiment Scheduler.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
